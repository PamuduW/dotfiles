# packages/packages.txt
# Section headers (# @tag) are used by install.sh to install subsets.
# Lines starting with # (without @) are plain comments.

# @core
git  # Distributed version control client
curl  # Command-line HTTP and data transfer client
ca-certificates  # Trusted certificate authority bundle
bash-completion  # Programmable completion definitions for Bash
bubblewrap  # Lightweight unprivileged sandbox helper
stow  # Symlink-farm manager for dotfiles
shellcheck  # Static analysis for shell scripts
shfmt  # Shell source formatter
tree  # Recursive directory tree viewer
build-essential  # Compiler, linker, make, and headers for native builds
pkg-config  # Compiler and linker flag discovery for installed libraries
cmake  # Cross-platform build-system generator

# @python
python3  # Python 3 interpreter
python3-pip  # Python package installer
python3-venv  # Python virtual-environment support
python3-dev  # Python headers for building native extension modules
pipx  # Isolated installer and launcher for Python command-line applications
python3-pil  # Python Imaging Library for image inspection and analysis
python3-lxml  # Python XML and HTML processing library
python3-openpyxl  # Python Excel workbook reader and writer
python3-docx  # Python Word document reader and writer
python3-yaml  # YAML parser and emitter for Python
python3-requests  # HTTP client library for Python
python3-bs4  # Error-tolerant HTML parser for Python
python3-jsonschema  # JSON Schema validation library for Python

# @cli
duf  # Human-friendly disk usage viewer
ripgrep  # Fast recursive text search
fd-find  # Fast filesystem search tool
fzf  # Interactive fuzzy finder
zoxide  # Smarter directory navigation
eza  # Modern directory listing tool
jq  # Command-line JSON processor
gh  # GitHub command-line interface
moreutils  # Additional Unix command-line utilities
git-lfs  # Git extension for versioning large files outside normal Git objects
tmux  # Persistent terminal session multiplexer
sqlite3  # Command-line interface for SQLite databases

# @system
lshw  # Hardware inventory utility
mtr-tiny  # Network route and latency diagnostic
glances  # Interactive system monitor
lsof  # List open files and owning processes
wslu  # WSL integration utilities
xdg-utils  # Desktop URI opener used by browser-launching CLIs
rsync  # Incremental file synchronization
unp  # Archive unpacking convenience wrapper
poppler-utils  # PDF inspection and conversion tools
libreoffice  # Office document conversion and headless rendering suite
pandoc  # Universal document and markup converter
docx2txt  # Quick DOCX-to-text extraction utility
xlsx2csv  # Excel workbook to CSV conversion utility
tesseract-ocr  # OCR engine for scanned documents and images
zip  # ZIP archive creation utility
unzip  # ZIP archive extraction utility
fontconfig  # Font discovery and substitution diagnostics
librsvg2-bin  # Exact-DPI SVG conversion with rsvg-convert
qpdf  # PDF page splitting, merging, inspection, and repair
ghostscript  # PDF and PostScript conversion, compression, and repair
inkscape  # Full SVG editing and conversion application
magic-wormhole  # Secure peer-to-peer file transfer
strace  # System-call tracer for process diagnostics
dnsutils  # DNS lookup tools including dig and nslookup
netcat-openbsd  # TCP and UDP connection testing utility
ffmpeg  # Audio and video conversion, probing, and streaming tools
imagemagick  # Raster image inspection, conversion, and manipulation tools
libimage-exiftool-perl  # ExifTool metadata reader and writer for media files
xmlstarlet  # Command-line XML query, edit, and transformation toolkit
